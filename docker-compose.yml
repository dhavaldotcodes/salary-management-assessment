services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: salary_management
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - acme_salary_data:/var/lib/postgresql/data

volumes:
  acme_salary_data:
